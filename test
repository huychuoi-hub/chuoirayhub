local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

-- Xóa UI cũ để tránh bị lỗi chồng giao diện
if CoreGui:FindFirstChild("HellocunProSelector") then
    CoreGui:FindFirstChild("HellocunProSelector"):Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HellocunProSelector"
ScreenGui.Parent = CoreGui
ScreenGui.IgnoreGuiInset = true

-- Khung chính (Main Frame)
local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.Size = UDim2.new(0, 340, 0, 200)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.BackgroundTransparency = 1 -- Sẽ hiện ra bằng hiệu ứng bung

local Corner = Instance.new("UICorner", MainFrame)
Corner.CornerRadius = UDim.new(0, 18)

local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Thickness = 2.5
Stroke.Color = Color3.fromRGB(255, 255, 255)
Stroke.Transparency = 1

local Gradient = Instance.new("UIGradient", Stroke)
Gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 255, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(160, 0, 255))
}

-- [QUẢN LÝ TRANG]
local function CreatePage(name, visible)
    local f = Instance.new("Frame", MainFrame)
    f.Name = name
    f.Size = UDim2.new(1, 0, 1, 0)
    f.BackgroundTransparency = 1
    f.Visible = visible
    return f
end

local MenuPage = CreatePage("Menu", true)
local KaitunPage = CreatePage("KaitunPage", false)
local HomePage = CreatePage("HomePage", false)

-- [HÀM TẠO NÚT]
local function MakeBtn(parent, text, pos, color, size)
    local btn = Instance.new("TextButton", parent)
    btn.Size = size or UDim2.new(0, 130, 0, 60)
    btn.Position = pos
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    btn.Text = text
    btn.Font = Enum.Font.GothamBold
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 14
    btn.AutoButtonColor = false
    
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 12)
    local s = Instance.new("UIStroke", btn)
    s.Thickness = 1.5; s.Color = color; s.Transparency = 0.5

    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.3), {BackgroundColor3 = color}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.3), {BackgroundColor3 = Color3.fromRGB(25, 25, 30)}):Play()
    end)
    return btn
end

-- [GIAO DIỆN TRANG CHỦ]
local Title = Instance.new("TextLabel", MenuPage)
Title.Size = UDim2.new(1, 0, 0, 60); Title.BackgroundTransparency = 1
Title.Text = "HELLOCUN SELECTOR"; Title.Font = Enum.Font.GothamBlack
Title.TextColor3 = Color3.fromRGB(255, 255, 255); Title.TextSize = 22

local GoKaitun = MakeBtn(MenuPage, "KAITUN", UDim2.new(0, 25, 0, 90), Color3.fromRGB(0, 160, 255))
local GoHome = MakeBtn(MenuPage, "HOME", UDim2.new(0, 185, 0, 90), Color3.fromRGB(255, 50, 80))

-- [HÀM XỬ LÝ ĐÓNG VÀ CHẠY SCRIPT]
local function CloseAndRun(url)
    local t = TweenService:Create(MainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0,0,0,0), BackgroundTransparency = 1})
    t:Play()
    t.Completed:Connect(function()
        ScreenGui:Destroy()
        loadstring(game:HttpGet(url))()
    end)
end

-- [TRANG CHI TIẾT KAITUN]
local KTTitle = Instance.new("TextLabel", KaitunPage)
KTTitle.Size = UDim2.new(1, 0, 0, 50); KTTitle.Text = "CHI TIẾT KAITUN"; KTTitle.Font = Enum.Font.GothamBold
KTTitle.TextColor3 = Color3.fromRGB(0, 160, 255); KTTitle.TextSize = 18; KTTitle.BackgroundTransparency = 1

local KTStats = Instance.new("TextLabel", KaitunPage)
KTStats.Size = UDim2.new(1, 0, 0, 40); KTStats.Position = UDim2.new(0, 0, 0.35, 0)
KTStats.Text = "Thời gian chạy: 00:00"; KTStats.Font = Enum.Font.Gotham; KTStats.TextColor3 = Color3.fromRGB(200, 200, 200); KTStats.BackgroundTransparency = 1

local KTBack = MakeBtn(KaitunPage, "QUAY LẠI", UDim2.new(0.1, 0, 0.7, 0), Color3.fromRGB(100, 100, 100), UDim2.new(0, 110, 0, 40))
local KTRun = MakeBtn(KaitunPage, "CHẠY NGAY", UDim2.new(0.55, 0, 0.7, 0), Color3.fromRGB(0, 255, 130), UDim2.new(0, 110, 0, 40))

-- [TRANG CHI TIẾT HOME]
local HMTitle = Instance.new("TextLabel", HomePage)
HMTitle.Size = UDim2.new(1, 0, 0, 50); HMTitle.Text = "CHI TIẾT HOME"; HMTitle.Font = Enum.Font.GothamBold
HMTitle.TextColor3 = Color3.fromRGB(255, 50, 80); HMTitle.TextSize = 18; HMTitle.BackgroundTransparency = 1

local HMError = Instance.new("TextLabel", HomePage)
HMError.Size = UDim2.new(1, 0, 0, 40); HMError.Position = UDim2.new(0, 0, 0.35, 0)
HMError.Text = "Trạng thái: Đang kiểm tra..."; HMError.Font = Enum.Font.Gotham; HMError.TextColor3 = Color3.fromRGB(200, 200, 200); HMError.BackgroundTransparency = 1

local HMBack = MakeBtn(HomePage, "QUAY LẠI", UDim2.new(0.1, 0, 0.7, 0), Color3.fromRGB(100, 100, 100), UDim2.new(0, 110, 0, 40))
local HMRun = MakeBtn(HomePage, "CHẠY NGAY", UDim2.new(0.55, 0, 0.7, 0), Color3.fromRGB(0, 255, 130), UDim2.new(0, 110, 0, 40))

-- [LOGIC CHUYỂN TRANG & THÔNG SỐ]
GoKaitun.MouseButton1Click:Connect(function()
    MenuPage.Visible = false; KaitunPage.Visible = true
    local start = os.time()
    task.spawn(function()
        while KaitunPage.Visible do
            local d = os.time() - start
            KTStats.Text = string.format("Thời gian chạy lâu: %02d:%02d", math.floor(d/60), d%60)
            task.wait(1)
        end
    end)
end)

GoHome.MouseButton1Click:Connect(function()
    MenuPage.Visible = false; HomePage.Visible = true
    task.wait(0.5)
    HMError.Text = "⚠️ Lỗi: Farm Bone thất bại!"; HMError.TextColor3 = Color3.fromRGB(255, 80, 80)
end)

KTBack.MouseButton1Click:Connect(function() KaitunPage.Visible = false; MenuPage.Visible = true end)
HMBack.MouseButton1Click:Connect(function() HomePage.Visible = false; MenuPage.Visible = true end)

KTRun.MouseButton1Click:Connect(function() CloseAndRun("https://api.jnkie.com/api/v1/luascripts/public/d26f5540308fedf1f069f5f48a4d1e8d426f399fbae4d1f6e882682d0c44e040/download") end)
HMRun.MouseButton1Click:Connect(function() CloseAndRun("https://api.jnkie.com/api/v1/luascripts/public/33902b2ab6354d9ec4bd3bc3fe3172ea6e68fa8ac1da0beac8ea22e2740d7973/download") end)

-- [ANIMATION KHỞI CHẠY]
MainFrame.Size = UDim2.new(0, 0, 0, 0)
TweenService:Create(MainFrame, TweenInfo.new(0.7, Enum.EasingStyle.Back), {Size = UDim2.new(0, 340, 0, 200), BackgroundTransparency = 0.1}):Play()
TweenService:Create(Stroke, TweenInfo.new(1), {Transparency = 0.1}):Play()

task.spawn(function()
    while MainFrame.Parent do Gradient.Rotation += 2; task.wait(0.02) end
end)

-- [HỆ THỐNG KÉO]
local dragging, dragStart, startPos
MainFrame.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = i.Position; startPos = MainFrame.Position
    end
end)
UserInputService.InputChanged:Connect(function(i)
    if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        local delta = i.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function() dragging = false end)
